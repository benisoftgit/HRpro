app = "hrpro"

kill_signal = "SIGINT"
kill_timeout = 5

[env]
  DEBUG = "False"
  SECRET_KEY = "4fecb98c7017ae795c5b666a4ea7d2ca"
  DB_NAME = "hrpro_db"
  DB_USER = "postgres"
  DB_PASSWORD = "Benisoft4477"
  DB_HOST = "localhost"
  DB_PORT = "5432"
  ALLOWED_HOSTS = "hrpro.fly.dev"
  FRONTEND_URL = "https://hrpro.fly.dev"
  CORS_ALLOW_ALL_ORIGINS = "False"
  EMAIL_USE_TLS = "True"

[deploy]
  release_command = "cd backend && python manage.py migrate --noinput"

[[services]]
  internal_port = 80
  protocol = "tcp"
  ports = [
    {handlers = ["http"], port = 80}
  ]
