"""Reports models — no database models needed; reports are generated on-the-fly."""

# Reports are generated dynamically from payroll, attendance, and employee data.
# No additional database models are required for this app.
